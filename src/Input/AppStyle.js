import { StyleSheet } from "react-native"
export const styles = StyleSheet.create({
    container: {
      flex: 1,
      backgroundColor: '#fff',
      justifyContent:"center",
      alignItems: 'center',
    },
    input:{
      width:'90%',
      borderWidth:1,
      borderColor:"black",
      marginBlock:20,
      borderRadius:10,
    }
  })